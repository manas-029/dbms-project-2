The route handlers are intentionally kept in `server/routes/` for a beginner-friendly Flask structure.

For a larger production team, request parsing can be moved from routes into controller modules here.
