This project uses Flask templates instead of React components because the requested frontend stack is HTML and CSS.

Reusable UI blocks live in `client/templates/dashboard/_sidebar.html` and the shared layout is `client/templates/base.html`.
